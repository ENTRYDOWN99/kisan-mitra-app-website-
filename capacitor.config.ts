import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.gov.agriculture.kisanmitra',
  appName: 'Kisan Mitra',
  webDir: 'www'
};

export default config;
